name: Build ARM64

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/reusable-build.yml
    secrets: inherit

    with:
      target: aarch64-unknown-linux-musl
      artifact: sshx-linux-arm64